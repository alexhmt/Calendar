<UserControl x:Class="OutlookCalendar.Views.MonthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:OutlookCalendar.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <SolidColorBrush x:Key="BorderGray" Color="#A5ACB5"/>
        <SolidColorBrush x:Key="GridLine" Color="#D4D4D4"/>
        <SolidColorBrush x:Key="TodayBackground" Color="#FFF8DC"/>
        <SolidColorBrush x:Key="WeekendBackground" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="OtherMonthForeground" Color="#999999"/>

        <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFFFFF" Offset="0"/>
            <GradientStop Color="#CFE4F7" Offset="0.5"/>
            <GradientStop Color="#B8CCE4" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ToolbarGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FEFEFE" Offset="0"/>
            <GradientStop Color="#ECE9D8" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Resources>

    <Grid Background="#F0F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель инструментов -->
        <Border Grid.Row="0"
                Background="{StaticResource ToolbarGradient}"
                BorderBrush="{StaticResource BorderGray}"
                BorderThickness="0,0,0,1"
                Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <RadioButton Content="День"
                             Margin="0,0,2,0"
                             Padding="10,4"
                             GroupName="View"
                             Command="{Binding SetViewModeCommand}"
                             CommandParameter="{x:Static vm:CalendarViewMode.Day}"/>
                <RadioButton Content="Неделя"
                             Margin="0,0,2,0"
                             Padding="10,4"
                             GroupName="View"
                             Command="{Binding SetViewModeCommand}"
                             CommandParameter="{x:Static vm:CalendarViewMode.WorkWeek}"/>
                <RadioButton Content="Месяц"
                             IsChecked="True"
                             Padding="10,4"
                             GroupName="View"
                             Command="{Binding SetViewModeCommand}"
                             CommandParameter="{x:Static vm:CalendarViewMode.Month}"/>
            </StackPanel>
        </Border>

        <!-- Панель навигации -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="{StaticResource BorderGray}"
                BorderThickness="0,0,0,1"
                Padding="8,6">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="◀" Width="24" Height="24" Command="{Binding PreviousWeekCommand}" Margin="0,0,2,0"/>
                <Button Content="▶" Width="24" Height="24" Command="{Binding NextWeekCommand}"/>
                <TextBlock Text="{Binding PeriodTitle}"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="12,0,0,0"
                           VerticalAlignment="Center"/>
                <Button Content="Сегодня"
                        Command="{Binding GoToTodayCommand}"
                        Margin="20,0,0,0"
                        Padding="10,2"/>
            </StackPanel>
        </Border>

        <!-- Сетка месяца -->
        <Grid Grid.Row="2" Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Заголовки дней недели -->
            <ItemsControl Grid.Row="0" ItemsSource="{Binding WeekDayHeaders}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1" Columns="7"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource HeaderGradient}"
                                BorderBrush="{StaticResource BorderGray}"
                                BorderThickness="0,0,1,1"
                                Padding="4">
                            <TextBlock Text="{Binding}"
                                       HorizontalAlignment="Center"
                                       FontWeight="SemiBold"
                                       Foreground="#333"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Дни месяца (6 недель x 7 дней = 42 ячейки) -->
            <ItemsControl Grid.Row="1" ItemsSource="{Binding MonthDays}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="6" Columns="7"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{StaticResource GridLine}"
                                BorderThickness="0,0,1,1"
                                Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftDoubleClick"
                                              Command="{Binding DataContext.SelectMonthDayCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              CommandParameter="{Binding}"/>
                            </Border.InputBindings>
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="White"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsToday}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource TodayBackground}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsWeekend}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource WeekendBackground}"/>
                                        </DataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsWeekend}" Value="True"/>
                                                <Condition Binding="{Binding IsToday}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Background" Value="{StaticResource TodayBackground}"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid Margin="2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Номер дня -->
                                <TextBlock Grid.Row="0"
                                           Text="{Binding DayNumber}"
                                           FontWeight="Bold"
                                           FontSize="12"
                                           Margin="2,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#333"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCurrentMonth}" Value="False">
                                                    <Setter Property="Foreground" Value="{StaticResource OtherMonthForeground}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsToday}" Value="True">
                                                    <Setter Property="Foreground" Value="#0066CC"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <!-- События дня (максимум 3) -->
                                <ItemsControl Grid.Row="1"
                                              ItemsSource="{Binding Events}"
                                              Margin="0,2,0,0">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="2"
                                                    Padding="2,1"
                                                    Margin="0,1">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#90EE90"/>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Category}" Value="Important">
                                                                <Setter Property="Background" Value="#FF6347"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Category}" Value="Business">
                                                                <Setter Property="Background" Value="#6495ED"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Category}" Value="Meeting">
                                                                <Setter Property="Background" Value="#FFFF96"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Category}" Value="Travel">
                                                                <Setter Property="Background" Value="#ADD8E6"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Category}" Value="Holiday">
                                                                <Setter Property="Background" Value="#FFA500"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Title}"
                                                           FontSize="9"
                                                           TextTrimming="CharacterEllipsis"
                                                           Foreground="#333"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>
