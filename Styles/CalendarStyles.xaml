<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:OutlookCalendar.Converters">

    <!-- Цветовая палитра Outlook 2003 -->
    <Color x:Key="OutlookBlue">#4A7EBB</Color>
    <Color x:Key="OutlookLightBlue">#B8CCE4</Color>
    <Color x:Key="OutlookHeaderBlue">#CFE4F7</Color>
    <Color x:Key="OutlookBorderGray">#A5ACB5</Color>
    <Color x:Key="OutlookGridLine">#D4D4D4</Color>
    <Color x:Key="OutlookWeekendBg">#F5F5F5</Color>
    <Color x:Key="OutlookTodayBorder">#F9B930</Color>
    <Color x:Key="OutlookToolbarBg">#ECE9D8</Color>
    
    <!-- Кисти -->
    <SolidColorBrush x:Key="OutlookBlueBrush" Color="{StaticResource OutlookBlue}"/>
    <SolidColorBrush x:Key="OutlookLightBlueBrush" Color="{StaticResource OutlookLightBlue}"/>
    <SolidColorBrush x:Key="OutlookHeaderBlueBrush" Color="{StaticResource OutlookHeaderBlue}"/>
    <SolidColorBrush x:Key="OutlookBorderGrayBrush" Color="{StaticResource OutlookBorderGray}"/>
    <SolidColorBrush x:Key="OutlookGridLineBrush" Color="{StaticResource OutlookGridLine}"/>
    <SolidColorBrush x:Key="OutlookWeekendBgBrush" Color="{StaticResource OutlookWeekendBg}"/>
    <SolidColorBrush x:Key="OutlookTodayBorderBrush" Color="{StaticResource OutlookTodayBorder}"/>
    <SolidColorBrush x:Key="OutlookToolbarBgBrush" Color="{StaticResource OutlookToolbarBg}"/>

    <!-- Градиенты для заголовков -->
    <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#FFFFFF" Offset="0"/>
        <GradientStop Color="#CFE4F7" Offset="0.5"/>
        <GradientStop Color="#B8CCE4" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="ToolbarGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#FEFEFE" Offset="0"/>
        <GradientStop Color="#ECE9D8" Offset="1"/>
    </LinearGradientBrush>

    <!-- Конвертеры -->
    <converters:TimeToTopConverter x:Key="TimeToTopConverter" HourHeight="60" StartHour="8"/>
    <converters:DurationToHeightConverter x:Key="DurationToHeightConverter" HourHeight="60"/>
    <converters:CategoryToBackgroundConverter x:Key="CategoryToBackgroundConverter"/>
    <converters:CategoryToForegroundConverter x:Key="CategoryToForegroundConverter"/>
    <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    <converters:BoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" Invert="True"/>
    <converters:PriorityToColorConverter x:Key="PriorityToColorConverter"/>
    <converters:DateTimeToTimeStringConverter x:Key="DateTimeToTimeStringConverter"/>
    <converters:DayOfWeekToRussianConverter x:Key="DayOfWeekToRussianConverter"/>
    <converters:TaskStatusToStrikethroughConverter x:Key="TaskStatusToStrikethroughConverter"/>

    <!-- Стиль кнопки навигации -->
    <Style x:Key="NavButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource OutlookBorderGrayBrush}"
                            BorderThickness="0"
                            CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource OutlookLightBlueBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource OutlookBlueBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Стиль кнопки вида (День/Неделя/Месяц) -->
    <Style x:Key="ViewModeButtonStyle" TargetType="RadioButton">
        <Setter Property="MinWidth" Value="60"/>
        <Setter Property="Height" Value="23"/>
        <Setter Property="Padding" Value="10,2"/>
        <Setter Property="Background" Value="{StaticResource ToolbarGradient}"/>
        <Setter Property="BorderBrush" Value="{StaticResource OutlookBorderGrayBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource OutlookLightBlueBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource OutlookHeaderBlueBrush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource OutlookBlueBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Стиль события календаря -->
    <Style x:Key="CalendarEventStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="3"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Margin" Value="2,1"/>
        <Setter Property="Padding" Value="4,2"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect ShadowDepth="1" Opacity="0.2" BlurRadius="2"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Стиль многодневного события -->
    <Style x:Key="MultiDayEventStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="2"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Margin" Value="0,2"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <!-- Стиль заголовка дня -->
    <Style x:Key="DayHeaderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource HeaderGradient}"/>
        <Setter Property="BorderBrush" Value="{StaticResource OutlookBorderGrayBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Height" Value="40"/>
    </Style>

    <!-- Стиль ячейки времени -->
    <Style x:Key="TimeSlotStyle" TargetType="Border">
        <Setter Property="BorderBrush" Value="{StaticResource OutlookGridLineBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Background" Value="White"/>
    </Style>

    <!-- Стиль задачи -->
    <Style x:Key="TaskItemStyle" TargetType="Border">
        <Setter Property="BorderBrush" Value="{StaticResource OutlookGridLineBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="4,2"/>
        <Setter Property="Background" Value="Transparent"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource OutlookLightBlueBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Стиль чекбокса задачи -->
    <Style x:Key="TaskCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,6,0"/>
    </Style>

    <!-- Стиль боковой панели -->
    <Style x:Key="SidePanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource OutlookToolbarBgBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource OutlookBorderGrayBrush}"/>
        <Setter Property="BorderThickness" Value="1,0,0,0"/>
    </Style>

    <!-- Стиль вкладки боковой панели -->
    <Style x:Key="SideTabStyle" TargetType="Border">
        <Setter Property="Width" Value="22"/>
        <Setter Property="Background" Value="{StaticResource ToolbarGradient}"/>
        <Setter Property="BorderBrush" Value="{StaticResource OutlookBorderGrayBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="0,3,3,0"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <!-- Стиль поля поиска -->
    <Style x:Key="SearchBoxStyle" TargetType="TextBox">
        <Setter Property="Height" Value="22"/>
        <Setter Property="Padding" Value="4,2"/>
        <Setter Property="BorderBrush" Value="{StaticResource OutlookBorderGrayBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <!-- Стиль RadioButton для переключателя недели -->
    <Style x:Key="WeekModeRadioStyle" TargetType="RadioButton">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="8,0,0,0"/>
        <Setter Property="Foreground" Value="#333333"/>
    </Style>

</ResourceDictionary>
